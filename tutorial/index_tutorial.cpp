#include <stdio.h>
#include <string.h>
#include <thread>
#include <vector>

using namespace index;

int main()
{
    // TODO
    // 对没有指定targetcode的定向 使用ALL定向填充


    return 0;
}

// g++ index_tutorial.cpp -g -oindex_tutorial -std=c++11 -lindex -lpthread -ldl -L/usr/local/lib64 -I/usr/local/include/index

